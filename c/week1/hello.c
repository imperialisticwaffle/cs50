#include <stdio.h>
int main(void)
{
    printf("hello, world");
}

/* To make the C code above executable, we need a compiler; in the CS50 IDE this is the command "make" entered at the CLI. We run the program with "./'filename'" */
/* Comments must be terminated */

