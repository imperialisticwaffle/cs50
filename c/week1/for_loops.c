#include <stdio.h>
#include <cs50.h>

int main(void) {
    for (int i = 0; i < 50; i++) {  // Sample for loop
        printf("%i\n", i); // Format characters pair with variables. Format characters come first
    }
}