/* JS is client side programming, meaning the code is still saved on the server side but is downloaded by the users locally and run from their computer */
let counter = 0;        // Let variable counter = 0
counter++;              // Increment counter by 1
let x = 1;
let y = 3;

if (x < y)
{
    console.log("ayy lmao");
}
else if (x > y)
{
    console.log("lmao ayy")
}
else
{
    console.log("bruh momentum")
}

while (true)
{

}

for (let i = 0; i < 3; i++)
{

}

/* With JS we can dynamically change our webpages in real time. We can do this by adding a script tag (<script></script>) to the head; it may also go under the body tag. There are some logical
differences between the two. You may also factor it out to another file using <script src="scripts.js"></script>. */